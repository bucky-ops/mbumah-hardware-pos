# ============================================================================
# MBUMAH HARDWARE POS & ERP - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in the values for your environment.
# NEVER commit your .env file to version control.
# ============================================================================

# Database
# SQLite for local development, PostgreSQL for production
DATABASE_URL="file:./db/custom.db"
# For PostgreSQL (production):
# DATABASE_URL="postgresql://user:password@localhost:5432/mbumah_pos?schema=public"

# JWT Authentication
# Generate a secure random string for production: openssl rand -base64 32
JWT_SECRET="change-this-in-production"

# M-Pesa Daraja API (Safaricom)
# Register at https://developer.safaricom.co.ke/ to get credentials
MPESA_CONSUMER_KEY=""
MPESA_CONSUMER_SECRET=""
MPESA_PASSKEY=""
MPESA_SHORTCODE="174379"
MPESA_ENVIRONMENT="sandbox"    # "sandbox" for development, "production" for live

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_CURRENCY="KES"     # Kenyan Shillings

# Notifications (Optional)
# Resend for email notifications
RESEND_API_KEY=""

# Twilio for SMS notifications
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_PHONE_NUMBER=""

# Redis (Optional — for production caching & sessions)
# REDIS_URL="redis://localhost:6379"
